# No AWS permissions needed yet (policy_arns = [] on irsa_frontend) — exists so
# the binding is already wired if the frontend ever needs AWS access later.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: frontend
  namespace: production
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::207137402976:role/carecloudly-production-frontend
