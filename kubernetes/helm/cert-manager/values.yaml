# cert-manager — installCRDs must stay true; ClusterIssuers depend on the CRDs
# this chart installs.
installCRDs: true
resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    cpu: 100m
    memory: 128Mi
